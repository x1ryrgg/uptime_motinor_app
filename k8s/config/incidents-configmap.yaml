apiVersion: v1
kind: ConfigMap
metadata:
  name: incidents-config
  namespace: uptime-monitor
data:
  DB_NAME: "uptimer_incidents"

  DJANGO_ALLOWED_HOSTS: "incidents.localhost,localhost,127.0.0.1"

  CORS_ALLOWED_ORIGINS: "http://localhost:3000,http://127.0.0.1:3000"

  CSRF_TRUSTED_ORIGINS: "http://incidents.localhost"

  USER_SUPPORT_GRPC_HOST: "user-support-grpc:50051"