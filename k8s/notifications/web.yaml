apiVersion: apps/v1
kind: Deployment
metadata:
  name: notifications-web
  namespace: uptime-monitor
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notifications-web
  template:
    metadata:
      labels:
        app: notifications-web
    spec:
      containers:
        - name: notifications-web
          image: uptimemonitor-notifications_web:latest
          imagePullPolicy: Never

          command:
            - gunicorn
            - config.wsgi:application
            - --bind
            - 0.0.0.0:8000
            - --workers
            - "2"

          ports:
            - name: http
              containerPort: 8000

          envFrom:
            - configMapRef:
                name: common-config
            - secretRef:
                name: common-secret
            - configMapRef:
                name: notifications-config
            - secretRef:
                name: notifications-secret

          readinessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10

          livenessProbe:
            tcpSocket:
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20

---
apiVersion: v1
kind: Service
metadata:
  name: notifications
  namespace: uptime-monitor
spec:
  type: ClusterIP
  selector:
    app: notifications-web
  ports:
    - name: http
      port: 8000
      targetPort: http